<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login // Admin</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="/assets/css/style.css" rel="stylesheet" />
</head>
<body class="bg-stone-100 text-black font-mono flex items-center justify-center min-h-screen p-4">
    
    <div class="max-w-md w-full">
        <div class="text-center mb-12">
            <h2 class="text-5xl md:text-7xl font-black uppercase tracking-tighter mb-4">
                LOCKED
            </h2>
            <p class="text-lg font-bold bg-[#FFDE59] inline-block px-2 border-2 border-black shadow-[4px_4px_0_0_#000]">
                AUTHORIZED PERSONNEL ONLY
            </p>
        </div>

        <form action="/admin/login" method="POST" class="border-4 border-black p-8 space-y-6 bg-white shadow-[8px_8px_0_0_#000]">
            <input type="hidden" name="csrf_token" value="<%= csrf_token %>">
            
            <% if (error) { %>
                <div class="border-2 border-red-600 bg-red-50 p-3 font-bold text-red-600">
                    <%= error %>
                </div>
            <% } %>

            <div>
                <label for="pin" class="block text-lg font-bold uppercase mb-2">Access PIN</label>
                <input type="password" id="pin" name="pin" required
                    class="w-full p-4 bg-white border-4 border-black text-lg font-mono placeholder-neutral-500 transition-all duration-150 focus:outline-none focus:bg-[#FFDE59] focus:shadow-[4px_4px_0_0_#000]" placeholder="****" />
            </div>
            
            <button type="submit"
                class="w-full px-8 py-4 bg-black text-white text-xl font-bold border-4 border-black uppercase cursor-pointer transition-all duration-150 hover:bg-[#FFDE59] hover:text-black hover:shadow-[4px_4px_0_0_#000] active:translate-y-1">
                Enter System
            </button>
        </form>
        
        <div class="text-center mt-8">
            <a href="/" class="font-bold underline uppercase hover:bg-[#5CE1E6]">&larr; Return to Site</a>
        </div>
    </div>

</body>
</html>
