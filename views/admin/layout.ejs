<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-50">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.10.5/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/assets/css/admin_dashboard.css">
</head>

<body class="h-full flex">

    <aside id="sidebar" class="fixed inset-y-0 left-0 bg-white shadow-md w-64 p-4 z-40 transform -translate-x-full lg:translate-x-0 flex flex-col">
        <div class="flex items-center pb-4 border-b mb-6">
            <i class="bi bi-shield-lock-fill text-indigo-600 text-2xl mr-2"></i>
            <span class="text-xl font-bold">Admin Panel</span>
        </div>

        <nav class="flex-grow">
            <a href="/admin" class="sidebar-link block py-2 px-4 rounded mb-1 text-gray-600 hover:bg-indigo-50 <%= title === 'Dashboard' ? 'active' : '' %>">
                <i class="bi bi-speedometer2 mr-2"></i> Dashboard
            </a>
            <a href="/admin/manage" class="sidebar-link block py-2 px-4 rounded mb-1 text-gray-600 hover:bg-indigo-50 <%= title === 'Manage Media' ? 'active' : '' %>">
                <i class="bi bi-collection mr-2"></i> Media Files
            </a>
            <a href="/admin/upload" class="sidebar-link block py-2 px-4 rounded mb-1 text-gray-600 hover:bg-indigo-50 <%= title === 'Upload Media' ? 'active' : '' %>">
                <i class="bi bi-cloud-upload mr-2"></i> Upload
            </a>
        </nav>

        <div class="border-t pt-4">
             <a href="/" target="_blank" class="block py-2 px-4 text-gray-500 hover:text-indigo-600">
                <i class="bi bi-box-arrow-up-right mr-2"></i> View Site
            </a>
             <a href="/admin/logout" class="block py-2 px-4 text-gray-500 hover:text-red-600">
                <i class="bi bi-box-arrow-left mr-2"></i> Logout
            </a>
        </div>
    </aside>

    <div id="sidebar-overlay" class="fixed inset-0 hidden lg:hidden sidebar-overlay"></div>

    <div class="flex-1 flex flex-col lg:ml-64 h-full">
        <header class="bg-white shadow-sm p-4 flex items-center lg:hidden">
            <button id="open-sidebar" class="mr-4 text-gray-600">
                <i class="bi bi-list text-2xl"></i>
            </button>
            <h1 class="text-xl font-semibold"><%= title %></h1>
        </header>

        <main class="flex-1 p-6 overflow-y-auto">
            <h1 class="text-2xl font-bold mb-6 hidden lg:block"><%= title %></h1>
            <%- include(contentPartial, { data: data, csrf_token: csrf_token, config: config }) %>
        </main>
    </div>

    <script src="/assets/js/admin.js"></script>

</body>
</html>
